# Modelo `ft_hibrido_legacy` (pré-protocolo v1)

Catálogo lógico do fine-tuning híbrido já executado antes da padronização v1.

## Observações
- Pode haver divergências entre script/relatório e checkpoint real (épocas, LR, eval/save steps).
- Deve ser usado como referência histórica, não como baseline principal do protocolo v1.
- Ver `runs/ft_hibrido_legacy/.../meta/run_manifest.json`.
